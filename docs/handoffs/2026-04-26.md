# Handoff: 2026-04-26 morning/early-afternoon

**Session focus**: Stand up the LLM-call mapping layer of the substrate
(static + runtime + cost + cross-reference), externalize pricing,
write decision ADRs.

**Pace**: Sustainable. Renaissance frame held — no timeframing, no
artificial scope compression. Bugs caught and fixed in flow.

## What ships today

Five bundles deployed cleanly to `spark-cfbd`:

1. **substrate-day2-bundle1** — foundation: `extractor_common.py`
   (shared backup-file filter, 17/17 self-tests pass) and schema
   migration creating `llm_call_events` + `substrate_ingestion_state`
   tables.
2. **substrate-day2-bundle2** — `extract_llm_calls.py` (AST-based static
   call-site extractor) + `cleanup_backup_entities.py` (no-op for our
   substrate; backup pollution wasn't actually present).
3. **substrate-day2-bundle3** — `ingest_token_usage_log.py` reading
   `/home/bot1/logs/token-usage.log` into `llm_call_events`. 129 rows
   ingested first run.
4. **substrate-day2-bundle4 v2** — model pricing externalized to
   `model.attrs.pricing` with verified-date provenance. `substrate_llm_queries.sql`
   v3 reads pricing from substrate, not hardcoded. Q13 added showing
   canonical pricing table.
5. **substrate-day2-bundle5** (this bundle) — five ADRs (0007-0011),
   re-extracted into substrate, this handoff doc.

## Substrate state

- 23 `llm_call` entities, 58 relationships
- 129 `llm_call_event` rows (token-usage log fully ingested)
- 11 `model` entities (10 with pricing seeded, 1 archive candidate)
- All ADR entities up to ADR-0011

## Real findings the substrate now knows

Captured as ADRs (linked) and queryable directly:

- **ai-synthesis Opus run-rate is ~$260/year**, not $540 (corrected
  pricing estimate). Q12 shows daily cost trend $0.29–$1.38, average
  $0.72. See ADR-0010 for why pricing is canonical in substrate.
- **Mac 1's qwen3:235b is at 0 runtime calls in 30 days** (Q11). The
  paused-system narration scripts haven't been firing per the captured
  window. Confirms "best hardware → most-reserved capacity, currently
  available for local-expert role." See ADR-0008.
- **S1 carries 79 of 130 captured runtime calls (61%)**. Most LLM
  load is on S1 + cloud Anthropic. S2 only 10 calls.
- **Static extractor missed Anthropic SDK call pattern** in
  ai-synthesis (Q9). Real gap; runtime data fills it. Captured for
  future extractor enhancement.
- **Local Ollama instrumentation broken** for token capture (Q7):
  every local-model call records 0 tokens. Anthropic captures correctly.
  `_log_tokens()` needs Ollama-shape support. Captured for future fix.

## Decisions made (ADRs 0007-0011)

| ADR | Decision |
|---|---|
| 0007 | Synthesis routing intentional: flow → S1/26b, intraday → S2/35B, ai-synthesis → conditional Anthropic Opus. Mac 1's 235B reserved for local expert. |
| 0008 | Defer Exo clustering. Mac 1 + Mac 2 = independent local-expert hosts. |
| 0009 | Local expert IS the reranker. No in-pipeline reranker layer. |
| 0010 | Substrate is canonical for rate-cards (LLM pricing, etc.). Code reads from substrate, not from memory or hardcoded. |
| 0011 | Verify schema before writing schema-dependent code. Three bugs in two sessions of the same class motivated this rule. |

## Bugs caught and fixed this session

- `relationships.kind` used in code; column is actually `type`
  (PATCH2 of extract_llm_calls)
- `args.model`, `prediction.get('model')`, `m.group()` etc captured as
  fake model strings (PATCH1, filter tightened)
- Shell endpoint regex grabbed Yahoo Finance URL as endpoint (PATCH1)
- `ON CONFLICT (cols)` needed full constraint, schema had partial index
  (DROP INDEX + ADD CONSTRAINT)
- Misremembered 11 backup variants of ai-synthesis as substrate
  pollution (verified empirically: 0; existing extractors already
  filtered. cleanup script became a no-op)
- Initial pricing estimates used Opus 4.1 rates ($15/$75) when actual
  is $5/$25. Self-corrected mid-session via web search. Pricing now
  canonical in substrate per ADR-0010.

## Pending / next session

1. **MCP server build (substrate-day2-bundle6)**. Stdio variant for
   Claude Desktop. Tools: `find_calls_to(model)`, `get_routing_for(script)`,
   `estimate_cost(window)`, `find_drift()`, `query_entity(name)`. Tier
   filtering. Audit log per ADR-0006.
2. **Mac Studio 2 setup** (in progress during this session — hardware
   work). Independent host per ADR-0008. Mirror Mac 1's config; no Exo.
3. **Network switch swap** to TL-SX1008 (8-port 10G). In progress
   alongside Mac 2 setup.
4. **Mac 1 local-expert LLM endpoint registration**: pending hardware
   completion + MCP server. Mac 1 hosts qwen3:235b (already loaded)
   as the local expert.
5. **Cleanup**: archive `qwen3.6:35b-a3b-s2` model entity (it's a
   label artifact, not a real model). Two-statement UPDATE; pending.
6. **`_log_tokens()` Ollama-shape support**: capture local-model token
   counts properly. Q7 surfaces this; future bundle.

## Operational

- `nvidia-smi` on S2 still broken (NVML mismatch; reboot required).
  Sunday window is appropriate for this if S2 isn't actively serving.
- Auto-load `/etc/neon-meta.env` in `~/.bashrc` — still recurring
  papercut. One-line add. Not done yet.
- ADR-0004 still says quant-research paused. Director scripts still
  fire daily but write to logs only (per Q11, no runtime evidence in
  captured window). Consider pausing the cron entries themselves
  until quant-research returns.

## Sentinels active after today

Yesterday's all carry. Added today:

- `SUBSTRATE_EXTRACTOR_COMMON_V1` (shared filter)
- `SUBSTRATE_V3_LLM_MAPPING` (llm_call_events schema)
- `SUBSTRATE_V3_MODEL_PRICING` (pricing in model.attrs)
- `SUBSTRATE_EXTRACT_LLM_CALLS_V1_PATCH2` (with both bug patches)
- `SUBSTRATE_INGEST_TOKEN_LOG_V1`
- `SUBSTRATE_LLM_QUERIES_V3`
- `SUBSTRATE_SCHEMA_VERIFY_BEFORE_WRITE_V1` (the practice rule from ADR-0011)
- `EMBED_NAN_BGE_M3_KNOWN_V1` → CLOSED (we migrated off bge-m3)

## Pace note

Yesterday spent significant time on embedding model evaluation. Today
shipped 5 bundles + 5 ADRs in one focused session. Different scope,
similar effort. Renaissance frame: don't compress horizons; do the
work that's right-sized for what's in front of you.
